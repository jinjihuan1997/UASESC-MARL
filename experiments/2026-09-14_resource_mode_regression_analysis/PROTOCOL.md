# 已完成长训练：资源—选模配合与退步阶段分析

本轮仅分析已有日志、轨迹和冻结模型，不新增训练，不采集新环境回合，不修改任何历史文件。

固定3个种子104948945、111868397、160441552；1M起点及2/4/6/8/10M不可变快照。固定任务使用各检查点已有20开发环境完整600槽轨迹，10M综合使用已有13场景统计。保留所有模型，不挑最优快照当正式结果。

第一部分：逐UAV核对Quality阶段资源份额、预算、16模式与原精确等价组、真实可行性、交付、AoI和资源使用。模式0对应的精确等价组与模式5对应组仅作为已有低载荷/中等载荷参照，不改变动作集。独立profile表重建用于区分“中档不可行”和“中档可行但选择低档”。无缓存、无模式时掩码全开回退单列。均分预算只做静态可行性计算，不执行资源替换，不把它当成闭环得分。

第二部分：在保存的真实分配后输入上复现各自模型原确定性动作，分析Categorical概率、模式等价组概率、熵与argmax余量。比较“概率最大单个编号所在等价组”和“总概率最大的等价组”是否一致，仅诊断，不更改原解码。原UAVGreedy可在同一局部输入、同一掩码和新权重上只读输出请求动作，用于分析分歧，不用其局部评分冒充真实团队奖励，也不进行标签训练。

第三部分：在各父种子固定10M轨迹输入上比较1M/4M/10M网络，分离网络变化与状态变化。计算原Dirichlet/Categorical的解析KL、资源输出差与模式分歧；这些是跨大段训练的漂移指标，不能与单次PPO更新阈值比较。没有运行替换网络后的闭环。

第四部分：合并1M前与长训日志，按1M窗口分析指令样本覆盖、各指令奖励均值、按指令/UAV的执行模式、熵、梯度范数、critic损失等可用字段。保留日志指标定义；原日志缺少逐actor/逐指令KL和clip fraction，不能从均值比率接近1推断更新幅度小。明确有限快照只能定位到观察区间，不能断言某一梯度步是唯一根因。

所有网络eval，参数与归一化状态冻结；禁止autograd、optimizer.step和环境物理推进。只在本目录写入脚本、统计、报告及核验记录。环境构造仅为获取冻结空间和外生profile表，不执行step或commit。只允许原开发种子白名单。质量为固定平均表预测PSNR，不外推实测视频解码性能。

保存输入SHA-256和各模型加载校验结果，分析后重新核对。所有配对统计按环境种子计算，模型和多UAV不当成独立环境样本。结果主要是关联与机制定位，不能取代后续受控闭环消融；本轮不自动启动消融或训练。
