本轮随机新增3个训练种子：810974、771611、779709。种子在训练及评价前一次抽取，排除已用种子；全部3个结果均须报告。已有85、218、966、268966的实验保持完整。

每个种子独立训练1000万步joint基础模型，再从本种子同一个完整检查点分别进行200万步选择器训练、200万步原方案续训。本轮包含3个基础训练任务、6个后续任务，共4200万新增物理步。训练源码与上一轮逐文件一致；所有算法、网络、奖励、SC平均质量载荷表、动作、指令生成、批量、折扣、终止规则及学习率/熵调度保持不变，只改变随机种子和版本化文件路径。

选择器组仍冻结原4个actor，使用原资源分配和均分资源两个候选，单独学习二选一策略并训练critic；对照组继续训练原4个actor及critic。候选选择不由指令固定路由。

基础阶段3个CPU任务并行。后续阶段6个任务排队，最多4个工作进程同时运行（训练和评估合计），优先执行等待中的训练任务。保持每个任务的CPU设备和固定核心。该安排基于既有吞吐测试与6路训练加评估曾过热的记录。温控参数保持：CPU>=85°C持续5秒、CPU>=95°C或GPU>=83°C触发更新边界保存暂停；最少冷却180秒，并在CPU/GPU都低于75°C持续60秒后恢复。只管理本队列拥有的进程。

基础模型只在1000万步完成时导出快照。3个基础模型完成后，自动验证各自迁移的权重、critic、环境状态、冻结actor和选择器log probability，再启动后续任务。每个后续方法在新增100万和200万步时评估，最后200万步是预定主要结果，两个检查点全部报告。

使用原13个场景、20个开发环境种子20262501–20262520，并重算3个固定规则基线：12个学习模型快照加3个规则，合计3900个完整600时隙评价回合。环境种子沿用已有开发集，因此不称为独立环境测试，也不据此证明隐藏/显式指令的独立贡献。

最终自动生成本轮3个新种子的单独汇总，以及保留全部7个种子的综合对比。旧结果不覆盖。

运行run_training.sh启动或恢复自动流程；暂停使用同一Python环境运行pipeline.py pause。主状态见status.json，分阶段状态见base/status.json、continuation/status.json。完成后结果见REPORT.md、comparison.json以及continuation/report目录。
