# AoI归一化参考10→8：固定三种子对照

用户指定 aoi_reward_ref=8。仅此科学参数发生变化：尾部阈值仍为4，质量收益仍计入交付数量，质量归一化、物理条件、指令权重、资源成本、网络和PPO参数均保留。8使平均、最大、尾部AoI成本同时增加25%，不是改变AoI状态上限或服务阈值。生产CPU、tensor、规则预测共用已有参数化奖励函数，不修改冻结旧实验。

先报告旧1M模型/旧规则的固定轨迹重新计分，明确这不是新训练结果。随后从头训练 ref8 的 IC_HAPPO、HAPPO_hidden_instruction，各种子85、218、966，各100万环境步，共6个新模型。ref10复用上一轮candidate_tail4的同预算末次模型，其导出权重复制并核对哈希，不伪称重新训练。两组配置仅参考值及快照输入路径不同；运行时源码保持逐字节一致。

预先固定新校准种子20262401—20262410，9个规则候选为模式0/5/10×均分/积压/缓存AoI分配。模拟一次物理轨迹，分别按ref10/ref8计分，对各自目标重新冻结单一规则和按指令选档规则。myopic使用对应目标的完整一步奖励。参数8不根据校准或测试排名再改动。

最终测试种子20262501—20262520；沿用13个固定/切换指令场景、600槽。两组各6个RL模型及3个规则，18对象×260回合=4680回合、2808000时隙。两套评分 objective_ref10/objective_ref8 都从同一物理轨迹独立计算。比较训练效果时必须使用同一评分列。主结果为末次确定性策略，不选择最优中途模型。

报告按训练种子聚合后的均值与样本标准差、ref8训练减ref10训练的配对差、显式减隐藏、RL减规则，以及固定指令/当前指令/切换前后分项。三种指令等权结果只作补充。不能把三种子正差当作统计显著性，也不能把评分单位变化当作物理性能改善。两个目标的基线共享物理约束，但规则与RL的观测信息/在线计算预算并非严格匹配。

6个CPU作业并行，核心0—5，每进程1数学线程。沿用已经验证的恢复与温控：CPU≥85°C持续5秒或≥95°C立即暂停，GPU≥83°C暂停；冷却至少180秒、两者低于75°C持续60秒恢复。只管理本实验进程。GPU正被其他项目使用，不修改其他项目进程。

启动前验证CPU/tensor、独立公式、一步规则预测、三个指令、非零处罚/接收项、隐藏输入、旧模型行为在两个参考值下相同、初始网络相同、短程训练可恢复。完成后重读全部保存轨迹，验证外生配对、指令时序、奖励分项、预算/质量/缓存约束和隐藏策略物理轨迹不变性。旧数据与所有冻结输入哈希保留。
