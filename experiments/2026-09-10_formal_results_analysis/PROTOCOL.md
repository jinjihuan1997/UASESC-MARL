本次为正式训练完成后的结果分析，2026-09-10；不是新的训练或预注册验证。

输入固定为 three_seed_sc_20260909 与 three_seed_cc_20260909 的最终 10M 步模型及全部评估。保留所有 3 个训练种子（85、218、966）、13 个场景、20 个评估种子，不挑选有利模型、场景或检查点。正式源代码、表、模型和历史结果均不修改。

先验证冻结输入、模型、报告和全部逐回合轨迹的 SHA-256，核对相同场景/评估种子的外生轨迹。独立重新聚合逐回合结果。固定指令场景另从逐槽 CSV 重新核算 AoI、交付量、质量、资源、模式及动作替换比例。

汇总先在每个训练种子内覆盖相同评估集合，再对训练种子求均值、样本标准差和范围。质量采用成功交付质量总和除以交付总数；资源除以总槽数。学习方法之间按训练种子配对；规则只有一个确定性策略，不能将其复制成三个独立样本。评估种子和指令场景复用外生轨迹，不能把数千回合当成数千个独立训练模型。本次报告描述性结果、配对差值和符号一致性，不宣称统计显著或模型最优收敛。

分析区分三个问题：系统是否响应指令；显式指令输入是否改善 RL；SC/CC 两种平均模型之间的系统差别。不同指令使用不同奖励权重，跨指令取舍用 AoI、成功交付质量和交付量解释，不直接比较不同指令的原始奖励高低。

在查看了正式 CC 的低交付率后，增加一个明确标注为“事后诊断”的 CC 规则评估：平均分配资源，选择当前满足质量要求且载荷最小的模式，沿用原 AoI 调度器和随机丢包协议。使用原冻结 CC 环境、全部 13×20 个评估条件、600 槽，不训练、不更改任何参数，不试多套规则择优。它用于判断已训练 CC 策略是否存在可避免的分配损失，不替代原正式结果。核对全部外生轨迹和 CC 丢包随机数哈希，独立检查每槽缓存、AoI、资源、期望质量可行性与共同奖励，并保存逐槽轨迹。该规则不构成最优 CC 上界。

对切换行为观察第 300 槽切换前 50 槽、切换后 50 槽及末尾 50 槽，并与相同槽位置的固定目标指令轨迹对照；这是描述性分析，不将状态不同的轨迹当作完全相同状态的因果干预。

科学范围继续为“固定 SC 平均模型与独立校准 CC 平均模型”；SC 编码器、具体视频和历史载荷实测绑定不足，以及双方传输成功抽象的差异，不因完成训练而消失。成功交付 PSNR 也不是所有时刻显示画面的质量。
