2026-09-10：资源选择器与joint续训对照，启动前冻结协议。

目标：检验RL能否学会在旧SUT分配和资源均分之间根据当前状态与指令自主选择。选择器不读取未来指令，不写死质量指令到均分的路由。该轮不用于确认显式指令的独立贡献。

每个种子85、218、966从相同1000万步joint完整检查点迁移，两种方法各追加200万物理步，共6个任务、1200万新增物理步。继承旧环境位置、外生序列、模型、critic、ValueNorm和已有Adam历史；新选择器单独初始化。新版本有独立身份和检查点，原1000万步实验保持不变。继承的完整检查点均已复制到本实验initial目录。

- joint_continue：继续训练原四个actor与集中critic；actor学习率2e-5线性降至2e-6，critic从1e-4降至1e-5，原熵系数0.008固定。
- selector：冻结原四个actor，且以确定性方式生成旧资源候选及UAV模式；增加一个两层64单元、Categorical(2)的可学习资源选择器。候选0为原SUT当前输出，候选1为原环境映射下的精确均分。选择器actor学习率1e-4降至1e-5，critic从1e-4降至1e-5，选择器熵系数0.008降至0.001。critic从原模型迁移后继续学习。只有选择器的二选一log probability进入PPO，不累计固定候选的概率。

选择器采用原PPO更新内核。10个环境×400时隙采样=4000物理步/次，500次更新；clip0.1，gamma0.99，GAE lambda0.95，5轮PPO/critic更新，2个小批次，最大梯度范数1。原joint保留HAPPO依次更新。每600物理时隙真实终止；分配资源不推进时钟，不产生额外奖励或折扣。资源选择后UAV观察实际预算再选模式。

固定原SC平均质量载荷表、AoI归一化参考值8、奖励权重、缓存/服务机制、模式可行性与回退规则。不得根据新结果调整这些因素或削弱基线。原资源范围没有变化。仅新选择器的动作表达改变。

新增100万和200万步时导出不可变推理快照，200万是预定主结果。自动在旧13场景×20环境种子上评价两个检查点的全部6个模型，并重算三个固定规则，共3900个完整600时隙回合。所有场景、所有预定检查点报告，不挑最好检查点。旧环境种子已用于方法诊断，本轮属于开发阶段。正式论文结论另需完整隐藏指令训练对照和参数冻结后的独立测试。

每25次更新及终止请求时保存原子完整检查点。暂停/恢复必须通过完整状态一致性预检。训练和评价使用进程身份核验与文件锁；只终止本队列拥有的进程。CPU>=85摄氏度持续5秒，或CPU>=95、GPU>=83时请求保存暂停；至少冷却180秒，且CPU/GPU都低于75持续60秒再自动恢复。温度传感器失效则队列报错并保存退出。该保护在本地训练队列中执行，不新增Codex定时监视。

CPU与GPU资源分配以本轮短时实测的整轮完成时间决定；同一种子的两种方法使用相同设备。保留6个训练工作槽，另外2个CPU核心执行快照评价。预计时长排除冷却与额外I/O开销，启动后按真实吞吐量更新。

启动：运行本目录run_training.sh。暂停：同一Python环境执行supervisor.py pause。恢复：run_training.sh --resume。过程状态见status.json，最终完整结果与核验见REPORT.md及report目录。
