本次按用户要求追加一个训练种子，预先固定为268966。抽取发生在新训练与评价之前，只抽一次，不根据成绩继续替换。原85、218、966三个种子的配置、模型、日志、结果表完整保留。

新种子没有旧基础模型，因此先按已验证的joint方案训练1000万物理步，再从同一完整检查点分别执行选择器训练200万步、原joint续训200万步。新增总预算1400万物理步，不把其他种子的旧模型改名冒充新种子的独立基础模型。源码逐文件与原两阶段实验核验一致；训练参数除随机种子外保持不变。

基础模型只在1000万步导出推理快照，学习率、网络、批量、环境、奖励及终止语义保持原1000万步训练口径。基础模型完成后，自动验证迁移权重、critic、环境序列和选择器概率，再并行训练两个200万步任务。新选择器仍只在旧SUT分配与均分资源之间学习选择；原四个actor在选择器组固定，续训对照组继续更新四个actor。学习率及熵系数的起止值与上一轮逐项一致。

每个后续方法在新增100万、200万步自动评价，200万步为预定主要结果。使用原13场景、20个开发环境种子20262501–20262520，重算3个规则基线；共1820个完整600时隙评价回合。两个预定检查点全部报告。原环境种子已多次用于开发，不称独立测试。

结果报告单列新种子，同时保留原三个种子和全部四个种子的汇总，不静默剔除表现不佳的966。当前966并非程序运行报错，而是其选择器在评估时始终选择旧资源分配；本次为补充随机性复验。

计算安排：基础训练1个CPU任务；之后2个CPU训练任务并行，另1个CPU工作槽负责评估。沿用既有温控、身份核验和断点恢复：CPU>=85°C持续5秒、CPU>=95°C或GPU>=83°C触发保存暂停；至少冷却180秒，并且CPU/GPU均低于75°C持续60秒再恢复。只控制本队列拥有的进程。

运行run_training.sh自动执行全流程。暂停使用同一Python环境运行pipeline.py pause；恢复重新运行run_training.sh。主状态见status.json，阶段状态见base/status.json和continuation/status.json。最终结果为REPORT.md和comparison.json。
