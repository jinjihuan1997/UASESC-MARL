# 质量收益半权：联合HAPPO长期训练延续

2026-09-14用户在确认短训练结束后要求开启长期训练。本实验明确将三个固定种子104948945、111868397、160441552从各自新权重100万步完整检查点继续至累计1000万步。每种子新增900万，三种子共新增2700万RL环境步。原100万步实验和全部历史文件保持只读；本次不是修改旧任务上限后冒称原协议，也不是重新随机初始化或仅热启动权重。

唯一研究变量是训练预算。质量收益系数仍为原值50%，三指令[质量,AoI,资源]为[0.25,0.4,0.1]、[0.10,0.7,0.1]、[0.35,0.2,0.1]，不重新归一化。SUT和三个UAV联合HAPPO，actor lr1e-4、critic4e-4恒定，10环境、400槽rollout、PPO5epoch×2小批次、clip0.1、entropy0.008、gamma0.99、GAE0.95，原动作头、归一化、critic/ValueNorm、优势归一化、物理、profile、掩码、资源下限、缓存调度及指令过程均保持原值。

从短实验最终update250的完整状态导入，保留actor、critic、ValueNorm、各优化器、buffer、RNG、环境、时钟、源回合索引和核验环形数组。允许改变的断点字段仅identity、config、total_updates；config仅改变预算、评估快照计划、输出标识和新审计路径。记录原始及新检查点SHA和所有元数据差异，逐值验证除此之外全部状态相同。原restore匹配约束不改；先产生与新协议匹配的迁移断点再严格restore。关闭学习率衰减，预算延长不会改变学习率。第一步必须与相同旧状态在短配置下直接执行的下一次更新逐值一致；保存/加载及跨600槽终止重放也必须一致。

训练从update251至2500，累计2500×4000=1000万步。每10万步保存完整可恢复状态，每累计100万步保存不可变推理快照（包括迁移起点100万步）。保存与原预算相同的日志、每指令计数、模式/预算、交付、奖励分项和梯度诊断。非有限错误停止，不换种子或伪造进度。所有新增训练时隙原物理/奖励核验；源回合175至1650每25回合保存完整10环境×600槽并独立重建，每种子60批360000槽，三个种子1080000槽。累计核验计数包含先前100万步，成本报告必须减去旧成本。

采用三个单线程CPU进程并行，避免更换设备破坏完整状态数值一致性。先实测本次续训吞吐，再根据实时资源分配核。现有其他项目只读检查，不终止或调整其进程。Torch/BLAS各1线程；PYTHONDONTWRITEBYTECODE=1，不安装升级依赖。无远程操作、论文修改或自动追加预算。

200/400/600/800万步各评估固定均衡、AoI、质量×20开发环境；固定1000万步评估原13场景×20环境×600槽。每种子500回合，三种子1500回合、900000评估步。全部确定性原动作解码，不挑最佳快照；阶段固定任务分数不称13场景综合分。评估通过开发种子白名单，所有训练派生种子与开发及reserved集合无交集，reserved_final_test从不运行。

复用短实验100万步结果及其原始轨迹，和同新奖励下旧RL、C1/C3、原规则、强贪心以及评分同步新权重的局部贪心。通过全部模型、配置、profile、场景、种子、外生序列和轨迹SHA认证后复用；不把旧目标分数搬来相减，不拟合新预测器。所有结果完整报告，包括落后种子。旧强贪心_Qhalf_local的SUT预测器仍使用旧拟合目标，保留其局限与无需拟合的G_equal_local16_Qhalf_local对照。

分数为每槽共同奖励均值×100，13场景、20环境、3个固定种子等权；bootstrap4000次，以环境为簇保留全部场景和模型关联，固定统计种子202609141300。全部开发证据不作最终测试或训练总体结论，平均表预测PSNR不作实测解码质量。最终全部评估逐槽和离线核验，重复数值聚合两次；确认受保护输入未变、实际种子与预留集无交集，然后报告并停止。
