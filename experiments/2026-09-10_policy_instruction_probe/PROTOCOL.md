2026-09-10：第一步，冻结模型的同状态指令输入干预。事后机制诊断，不是训练或性能优越性检验。

输入：three_seed_sc_20260909 的最终 10M 步 IC_HAPPO、IC_MAPPO，训练种子 85、218、966；对应的六个隐藏指令模型作为阴性对照。正式输入、模型和历史轨迹保持只读，记录 SHA-256。

状态来源：六个完整模型的全部 13 场景 × 20 评估种子 × 600 槽原始正式轨迹。用记录的原始资源动作和提出模式重放冻结张量环境；逐槽核对原轨迹的执行模式、资源、AoI、交付量、质量和奖励。每 10 槽取一次决策前状态，并加入所有任务切换点的前一槽和后一槽（切换点本身已在取样中）。三个训练种子均保留，场景和评估种子不按结果筛选。

对每个相同状态构造 11 种输入：原始输入；显式指令编号及其质量/AoI 门限一起显示为 0/1/2；只替换编号为 0/1/2；只替换门限为 0/1/2；编号及门限全部置零。门限按当前物理 SNR 桶计算。除指定显式输入坐标外，其他观察逐位相同；真实任务、奖励规则、当前预算、可行动作掩码和由真实任务生成的待传载荷观察均不修改。始终继续执行历史记录中的动作，不让反事实动作改变后续状态。

隐藏指令模型在同一批物理状态上接受正确置零后的观察；11 个名义干预均仍为零，不把训练时没见过的非零指令塞入隐藏模型。验证其输出严格不变。这只校验干预隔离，不是新增隐藏模型的闭环性能评估。

记录：UAV 筛选前/后模式概率、提出模式、SUT 最终资源份额。主要描述“显示 AoI”与“显示质量”两种完整任务输入的差值；辅助区分编号和门限的影响。报告概率分布总变差距离、模式编号改变比例、排除相同质量/载荷重复行后的有效模式改变比例、资源份额绝对变化及至少一个 UAV 改变 1 个百分点的状态比例。按每个训练种子独立汇总，再给均值、范围；不把大量状态当作独立训练模型。不设显著性结论。

边界：错误编号、错误门限或零输入与真实掩码/其他观察可能形成训练分布之外的组合。这是固定其他因素后检测显式输入影响的机制实验。输出变化说明策略函数对这些输入敏感；不能单凭敏感性推断收益、正确理解、必要性或新环境中的泛化。动作不变也不能排除概率变化及其他状态上的作用。本轮不改变真实门限或动作筛选，不重训练，不宣称分离出整个指令系统的所有因果贡献。

数值实现：不同干预逐组采用完全相同的批次大小和行位置执行推理，防止批次位置引起的浮点差异被计作指令影响。初始实现的阴性对照在 fixed_0 第 210 槽发现资源份额差约 1.98e-8，故在正式完成任何场景前统一推理布局。第二次检查在 MAPPO/218、fixed_1、第 380 槽、评估种子 20261305 出现批量 argmax=12 而历史 argmax=8，两者质量/载荷完全相同，概率差约 3.87e-7；用原始单环境批次重算恢复历史模式。最终统一规则为：若原始输入的批量模式不匹配历史单环境推理，对该状态的所有干预和阴性对照都恢复单环境批次，并验证原始模式与历史一致。记录这些状态及批次大小，不因接近打平的编号差异绕过核验。保留前两次开发检查，并用最终同一脚本重跑六组；未据干预效果修改实验条件。

资源：两个独立 CPU 工作进程，各 1 个数学库线程，HAPPO 与 MAPPO 分别处理；GPU 无需参与。
