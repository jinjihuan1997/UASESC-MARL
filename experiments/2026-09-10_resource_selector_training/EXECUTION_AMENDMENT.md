首次训练因CPU达到90°C触发原温控，全部任务已在更新边界保存。本次仅把外部调度上限改为4个同时运行的工作进程（训练与评估合计），优先执行等待中的训练任务；其余任务排队。保留温度阈值、至少180秒冷却及低于75°C持续60秒条件。六个任务的算法、随机种子、CPU核心、设备、总预算、检查点身份与评估协议完全不变。旧manifest及源码保持不变。

现在使用run_training_adjusted.sh恢复队列；状态仍见status.json。暂停使用supervisor_adjusted.py pause。
