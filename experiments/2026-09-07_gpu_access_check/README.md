GPU 可正常使用。2026-09-07 的排查确认：受限执行环境的 /dev 中没有 nvidia 设备节点，因此其中的 nvidia-smi 无法访问驱动，torch.cuda.is_available() 返回 False；在主机环境执行相同检查则成功。

实际验证的主机配置为 NVIDIA GeForce RTX 4070 Ti SUPER（16,376 MiB），驱动 595.84，项目环境 PyTorch 2.4.1+cu121、CUDA 构建版本 12.1。主机上的 cuda:0 矩阵乘法成功且数值有限；HAPPO 短训练也实际使用了 GPU，运行时显存占用约 404 MiB。无需重新安装现有驱动或 PyTorch。

使用方式是让训练进程运行在可访问显卡的主机环境中，并设置配置字段 algo_args.device.cuda = true。本次通过已批准的主机执行方式完成了测试；configs/gpu_seed1.json 保存了测试配置。仅打开 cuda 开关而不解决设备可见性，项目现有 init_device() 会回退到 CPU。

| 同一代码、10 个环境、seed 1、8,000 步 | CPU | GPU |
| --- | ---: | ---: |
| 含初始化总耗时（秒） | 18.4673 | 36.8083 |
| 平均环境步/秒 | 433.20 | 217.34 |
| 4 个 actor 均更新 | 是 | 是 |
| 参数与训练指标均为有限数值 | 是 | 是 |
| 保存的模型文件数 | 6 | 6 |

这是依次执行的各一次短测，期间原来的正式 CPU 训练持续运行。它证明当前代码能够用 GPU，也显示此次短测没有加速；不用于比较收敛效果或推断长期稳定速度。模拟环境仍在 CPU 上执行，性能瓶颈的精确归因需要另做剖析。

正式 A/B 队列保持原有冻结配置继续运行。若之后要改变设备、批量或训练实现，应建立新的实验版本，确保 A/B 使用相同设置，并保留此次记录。

diagnosis.json 保存设备访问诊断；benchmark.json 保存短测汇总；cpu.log、gpu.log 和 jobs/ 保存原始训练输出、初始/最终参数哈希与检查点哈希。此次未修改驱动、系统权限配置、冻结的正式训练代码或正式训练配置。
