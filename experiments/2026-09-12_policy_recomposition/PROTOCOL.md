# 冻结控制器重组与质量资源替换：执行前协议

创建日期：2026-09-12（Asia/Tokyo）。这是已反复使用的开发验证集上的手工控制器设计检验，不是独立最终测试。上一轮全指令修正固定均衡有收益，质量门控固定质量有收益但落后规则；不预设本轮成功。父种子固定104948945、111868397、160441552，各自joint原100万步及两种adapter追加100万步最终快照，禁止挑选/跨父组合。

## 封存定义

|控制器|均衡资源/选模|AoI资源/选模|质量资源/选模|
|---|---|---|---|
|C0|原SUT/原UAV|原SUT/原UAV|原SUT/质量有效策略|
|C1|原SUT/全指令有效策略|原SUT/原UAV|原SUT/质量有效策略|
|C2|原SUT/原UAV|原SUT/原UAV|原均分动作/质量有效策略|
|C3|原SUT/全指令有效策略|原SUT/原UAV|原均分动作/质量有效策略|

有效策略复用上一轮ResidualPolicy完整实现；不相加adapter，不更改掩码，确定性argmax。SUT用原Dirichlet均值解码。指令名核对原环境instruction_names；SUT局部23:26、UAV局部67:70。逐样本路由，网络只见原本观测。均分动作调用原simple_rule_actions(R_equal_instruction)的资源部分；native allocator先将动作转float64再加1，份额下限仅一次。均分时不先按原预算选模。单次allocate、重新observe、单次commit/奖励/时钟；切换不断开缓存及AoI。原actor前馈配置必须验证。

## 固定范围与复用

manifest封存父实验13个精确场景、20个validation种子、600槽，不新增种子。C1/C2/C3每种780完整回合；主要新增2340回合/1,404,000物理步。C0复用residual_quality最终780回合；另复用original_rl、residual_all最终、quality_rule_hybrid各780，以及R_instruction、R_equal_instruction、greedy_modes_3、greedy_modes_16各260。共4160回合复用，按旧manifest、checkpoint状态、输入SHA、轨迹SHA及已通过审计核验；缺项正式运行前列明，不能搬历史均分。基线无父模型，广播展示不算独立样本。

预检每父模型×C0/C1/C2/C3×fixed_0/fixed_1/fixed_2/switch300_0_to_1/switch300_1_to_2，共1200完整回合。另C0三父×multi_2_1_0共60回合；C3首父switch300_1_to_2重复一次20回合。计划预检1280回合/768,000物理步，不冒充正式新增。首个完整预检轨迹测吞吐，混合batch路由及分配检查不推进物理时钟。任何失败尝试另计，保留错误。C0其余旧轨迹由保存的局部状态复建输入进行冻结策略复核及全部旧轨迹输入输出哈希审计。预检严格相等，不放宽容差。

所有环境创建通过开发种子白名单：受控EpisodeSource构造只替换种子绑定，继承原next及全部物理实现；连初始化的seed(None)也绑定获准种子。不得运行reserved_final_test，最终检查实际trace种子与其交集空。

## 预检与审计

固定均衡C1=C3=residual_all，C0=C2=原策略；固定AoI四种=原策略；固定质量C0=C1=门控，C2=C3且真实均分。无质量场景C0=C2/C1=C3；无均衡场景C0=C1/C2=C3。C0多次切换复现旧门控。异质batch逐样本路由；资源阶段时钟、缓存、AoI不变；同种子重复一致；加载顺序不影响外生；全部参数、buffer/归一化状态哈希不变。

CPU float32网络/float64环境、Torch/BLAS单线程，先少量并发按实测选最多3进程；保持旧CPU算术重现。不终止他人进程。所有新时隙调用原checked_step，再调用修正后的上一轮独立NumPy physical_audit，从profile/资源/缓存/时间戳重建模式可行性、回退、稳定AoI调度、交付、AoI及全部奖励。物理AoI及份额1e-12、载荷1e-8、共同奖励1e-9、float32奖励原1e-6，确定性复现0容差。

## 原始数据及统计

记录原22项trace、动作、份额/预算/使用/未用、请求与执行模式、服务矩阵、AoI/缓存/时间戳、原掩码，增补SUT观测、分配后各UAV局部观测、每样本资源/策略来源；模型SHA通过封存索引映射。完整外生序列及初态哈希逐环境保存。记录当前决策指令而非下一时隙。

原始16模式不变；精确等价组由profile三项表逐值相等生成。模式0组[0,4,8,12]和模式5组[5,9,13]仅作为旧文中“低/中”指代，不新划档。未交付独立统计，各比例写分母。按场景×当前指令×父模型×UAV报告资源、模式、交付、预测质量及AoI尾部；预算可行/最终可行分别统计。重点预指定160441552的UAV3，比较C2-C0及C3-C1质量阶段，区分模式5组预算不足和可行仍未选择。

评分每槽原共同奖励平均×100，保留原始量。13场景、20环境、3父模型等权。预先固定bootstrap seed=202609122122，4000次环境成组重采样，同一次索引保留全部场景/父模型/方法相关性。区间仅以当前三父模型及开发环境为条件；所有比较包括负结果全部展示。

对综合、三个固定场景、10切换场景和每场景报告C1-C0、C2-C0、C3-C0、C3-C1、C3-C2与I=C3-C1-C2+C0，逐父及条件平均；比较三新方法与全部参考。I是执行改动闭环交互，不是训练根因比例。

完整13场景按实际当前指令占比加权分解剩余分差，分解必须加回综合差；保留正负项，不用固定3场景或指令简单三等分。预测PSNR总交付质量/总交付数；它是固定平均表预测，非实测解码。质量结束后非质量片段重点C2-C0/C3-C1，报告均分影响遗留；不重复从整回合扣成本。

## 只读、复算与停止

全部输出仅新目录，PYTHONDONTWRITEBYTECODE=1，全部eval/inference，禁止backward/autograd/optimizer.step，不实例化critic或优化器。新训练步=0、新优化器更新=0。C0/C2每父已有2M谱系成本，C1/C3每父已有3M；全三父依赖共9M历史训练步（共享父不重复计）。不改变奖励/profile/物理/动作/调度/约束，不改论文、不commit/push、不拟合贪心，不自动扩大方法或追加训练。

所有文件原子写入；仅封存协议、代码、模型及输入哈希相同时复用本轮完成项。运行后再核对全部保护输入；两次完整聚合数值应相同，时间元数据分开。交付完整逐模型中文结论、负结果、计算账本、核验报告；完成固定评估即停止，只建议下一步。
