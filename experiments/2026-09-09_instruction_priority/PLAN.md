新模型 instruction_priority_v1，2026-09-09

本版本保留用户指定平均表和原正式训练。Manuscript中仅新增一个修订tex，不创建稿件子目录。实验源码和新结果独立版本化。

固定内容：3 UAV、30 DS、600槽、每槽1秒、阻塞单缓存、共同AoI优先调度、表SHA a6f721686b28d6c28ccf811ab8b63e80ae8c135a08b9d7e1b4bbfe7ca13d91cc。更新单元按最终表标识声明为256分辨率RGB8帧抽象更新，不声称逐视频实测。

修改：Dirichlet直接映射可分配余量；独立AoI奖励参考10秒；权重按[质量,AoI,载荷]为[.5,.4,.1]/[.2,.7,.1]/[.7,.2,.1]；最大AoI软目标[6,4,10]；aoi质量门限[21.5,24,25,25.5]；另外两个质量门限保持。物理带宽、总功率不动，可用资源比例eta=.6，故不会额外提高信道SNR。

验证顺序：CPU与张量环境在7条件及5边界条件下对照；实际GPU重复同一对照；资源映射/奖励独立算式检查；R_fixed参数可行性检查；短训练与完整checkpoint恢复；通过后才准备单种子pilot。

参数开发集固定为20260911、20260912、20260913。完整保留eta=.5/.6/.75/1四点，三种固定指令各600槽；不按算法优势选点。预定eta=.6通过条件：每开发种子有交付，aoi超限比例<1%，balance/quality<5%。门限不满足时停止启动并报告，不能暗中放宽。

Pilot：训练种子85；IC_HAPPO与HAPPO_hidden_instruction；各1,000,000步（250更新），相同GPU设备、同初始化种子、10环境×400步、既有PPO超参数。两任务并行，共2M步，是短预算可行性研究，不替代10M×3种子的正式结论。训练参数中没有先前模型目录。

评价：现有13条固定/切换场景，5个新的外部种子20260921至20260925，600槽；同外生过程配对。对照显式输入与隐藏输入，后者保留共同服务约束及间接信号。报告任务条件收益、AoI、预测PSNR、交付数、开销、违规、未见场景及不利结果。一个训练种子不作训练稳定性或显著性结论。

训练局部温控：CPU>=85C连续5秒或GPU>=83C时在PPO更新边界保存并暂停；冷却至少180秒且CPU<75/GPU<75持续60秒后恢复；不创建或恢复应用中的持续监视自动化。完整输出包括源码哈希、配置、完整恢复状态和最终权重。环境/目标变更与旧正式实验分别报告。
