# GitHub发布范围：核心代码与汇总数值

用户于2026-09-14明确修正发布范围。本规则替代此前的完整实验快照及“不超过10MiB模型可上传”约定。

保留：核心源码、运行与复算入口、必要配置和依赖清单、许可证、协议、主要汇总数值、配对统计、简明报告与审计。为保留正负证据，旧实验主要汇总也保留；不根据结果筛选实验或种子。

排除：所有大小的已学习权重、actor/critic/adapter/ValueNorm/预测器、检查点、优化器及RNG/环境恢复状态；原始数据集、监督/DAgger样本、逐槽轨迹和密集数组；逐步训练日志、大型历史分布、完整工作区差异转储；视频、图片、论文附件、二进制引擎、缓存，以及每个运行重复的源码快照。

必要的小型物理质量—载荷查表是显式例外，按清单审查其内容与哈希，不能将预测器参数伪装成查表。当前保留表最大约14KB，总量约282KB。普通文本和汇总结果仍须检查内容；当前单文件发布上限8MiB，超过时保留本地完整版本与已发布主报告，不自动压缩后绕过限制。

每次发布生成明确清单，审核新增/删除与最终Git树；`.gitignore`仅为辅助。`PUBLICATION_MANIFEST.json`包含受审查文件的路径、字节数、SHA-256、Git blob、文件模式和类别，其自身不递归列入。`scripts/check_publication.py`核对暂存区或HEAD，不自动批准新文件。

本地原件及未提交修改不因远程清理而删除。禁止将需要本地权重或原轨迹才能复算的代码称为完整自包含恢复包。普通删除只更新当前分支；历史清除必须另行明确处理，不能声称历史对象已经移除。
