# 新质量权重下从头重训 joint HAPPO

用户要求开启新权重设计下的RL重训。三个种子固定104948945、111868397、160441552，沿用原joint HAPPO的100万环境步/种子，总计300万新RL环境步。从随机初始化开始，CPU初始化哈希必须与对应旧joint实验完全一致；不加载旧权重或优化器，不用adapter、教师模仿或新KL约束，不重新训练C1/C3组合。

唯一目标设置变化：三指令[质量,AoI,资源]系数为[0.25,0.4,0.1]、[0.10,0.7,0.1]、[0.35,0.2,0.1]。只质量项减半，不归一化；AoI、资源、物理、平均表、阈值、16模式、掩码、缓存调度与指令生成全部保持原值。原构造器行和校验保持不动，在独立环境实例中设置有效奖励系数，训练、核验、评估统一使用它。

10并行环境、rollout400，每次4000环境步，250次采集/更新；actor lr1e-4、critic lr4e-4，恒定；PPO/critic epoch5，各2小批次；clip0.1、entropy0.008、gamma0.99、GAE0.95、grad norm1；网络256×256、原特征归一化和Dirichlet/Categorical动作结构、全局团队奖励、原HAPPO顺序与概率比聚合均不变。四个actor和原结构critic全部正常训练。

每槽SUT从当前策略采样资源，实际分配后重建UAV局部观测/掩码，再从UAV当前策略采样，物理只推进一次。600槽真实终止；指令在100–500槽随机切换，切换不是done、不截断GAE。外生环境仍使用独立的原SeedSequence流，训练环境为master+1000*i，i=0..9，逐一检查与开发/预留种子不重叠。动作与更新顺序沿用原Torch随机流，不改变原训练内核的随机数组织。

原文件、配置、模型、质量表、论文和报告只读。全部新代码、日志、检查点、配置、评估与报告写本目录，禁止git reset/clean/commit/push、论文修改、最终测试和额外训练。PYTHONDONTWRITEBYTECODE=1，不安装或升级共享环境。

CPU/CUDA先执行相同短程吞吐测试与更新检查，再仅依据速度和当前资源选择执行设备；最多3个训练进程，Torch/BLAS各1线程，不终止其他项目。CPU是与旧模型初始化直接配对的首选，GPU只有通过预检且明显提高整轮完成时间时使用。正式资源决策写resource_plan.json，不根据验证得分改变。

每10万步保存可恢复完整状态，每20万步保存不可变推理快照。检查点含actor、critic、ValueNorm、全部优化器、buffer、RNG、环境/采样进度及训练物理核验环形数据；只恢复本目录同配置/协议/设备任务，不能借resume扩大100万步预算。失败停止，不静默修复NaN或换种子。原有限值路径不变，遇到旧nan_to_num即将替换非有限输入时提前报错。

预检：三CPU种子初始化哈希匹配；质量半权、原观测和动作/掩码一致；单次物理推进、GAE不跨600槽终止、采样/evaluate_actions logp及初始ratio一致；真实actor与critic更新且finite；跨终止检查点恢复逐值一致；CPU与CUDA相同短程吞吐测试。预检更新仅诊断，正式从零重新开始，单独记成本。

所有训练物理槽调用原独立核验器。每第25个完整物理回合波次（含0）保留10环境完整600槽轨迹并用独立NumPy信道/profile、执行器、AoI、缓存与奖励重建。预期正式每种子7波次、42000槽离线全轨迹覆盖；每槽在线核验覆盖全部100万步。记录真实指令样本、模式/预算分布、资源余量、交付、奖励分项、损失/熵/梯度/概率比和完整采样进度。

预定评估：20/40/60/80万步各评估3固定任务×20开发种子；固定100万步评估原13场景×20种子×600槽。三个模型共1500评估回合、900000物理步。评估全确定性，用原动作解码；中间固定任务不称13场景综合分。最终主要比较100万固定检查点，不挑最优快照。

对照复用已认证2026-09-13_quality_weight_half的新目标轨迹与数值，包含旧RL、C1、C3、全部原简单规则、冻结强贪心，以及局部评分同步新权重的贪心。特别保留无需拟合的G_equal_local16_Qhalf_local。旧强贪心SUT预测器仍按旧目标拟合的局限不隐瞒。不跨奖励口径直接比较历史分数。

统一分数为每槽共同奖励均值×100；13场景、20环境、三个固定种子等权。按环境种子成簇配对bootstrap4000次，固定统计seed202609141225，保留13场景关联。逐模型和条件平均均报告，不把3个种子外推训练总体；开发验证不是最终测试，PSNR仅平均表预测。训练后自动完成固定评估、独立核验、重复数值聚合与中文报告，然后停止，不继续降低权重或延长预算。
