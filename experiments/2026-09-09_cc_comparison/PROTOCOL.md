CC 补充训练协议，2026-09-09。

本轮训练 CC_IC_HAPPO、CC_IC_MAPPO，对应正在运行的 SC IC_HAPPO、IC_MAPPO。种子固定为 85、218、966，每组从随机初始化训练 10000000 个环境步。网络、优化器、学习率衰减、批次（10 环境×400 步）、600 槽回合、指令切换、质量目标、AoI 目标、奖励权重、链路和资源参数全部来自 SC 已冻结配置。表内 20 个模式均为独立动作，没有按 SC/CC 性能筛选模式。

新 CC 输入为先前重建的平均表，SHA-256 为 1577cd5ee5cfe0f74c325f42d32e59fe5f417c13bc1f51a4539d2b4addad7a96。96 段校准片段生成成功概率及质量、奖励的联合矩；48 段验证片段不参与训练表。保持平均载荷建模，不在 RL 中悄悄切换为随机片段载荷。每次发送计入完整码块/包头的平均复信道使用次数，不增加 SC 的语义侧信息，也不再除 Shannon 速率。

预决策质量门限与观察使用表的期望 PSNR，规则与 SC 相同。每个 DS、每槽有独立于位置、信道、内容、指令和策略随机数的均匀随机数，按该模式/SNR 的平均整包成功率决定是否成功。同一训练种子/回合/槽/DS 在 CC 方法间使用相同外生随机数。先插值成功概率及质量联合矩，再求总体条件质量；不以平均成功率乘无损平均质量近似联合期望。额外的随机数不暴露给 actor。

使用单次发送、无 ARQ 的块丢弃协议。一次发送尝试即消耗资源并在槽末释放该缓存，成功和失败都不在本槽立即补入新数据。成功接收时才按生成时间更新 RCC AoI；失败时 AoI 增加一个槽。未尝试的缓存继续保留。该定义保持原 SC 的发送/采集时序，也与 CC profile 的每次发送无重传统计一致。它不模拟包头实际漏检、跨槽重传、端到端编码计算时延或真实损坏 H.264 解码。

共同奖励继续使用按成功接收更新的质量增益、所有发送尝试的载荷成本、接收端 AoI 和原指令约束奖励。失败不获得成功接收质量增益。33 dB 是质量归一化分母的参考值，并非上界；原 SC 代码只在零处截断，33 dB 以上可获得大于 1 的归一化质量。该行为保持不变。质量增益联合矩也先对片段逐一计算，再平均，避免非线性平均误差。

最多 3 个 CPU 工作进程，固定核心 0、4、6，每进程 1 个数学库线程；GPU 和 SC 的 CPU 核分配不变。4 路 40k 短测峰值 85°C，且 6 组任务采用 3 路和 4 路均需两批；据此采用 3 路分配，未依据策略表现改变方案。训练及评估使用相同 CPU 张量环境。每 25 次更新保存完整训练状态，温控、人工暂停和错误处理继承已经验证的队列机制。CPU 持续 85°C 5 秒或达到 95°C、GPU 达到 83°C 时停止本 CC 队列，至少冷却 180 秒且均低于 75°C 60 秒后恢复。程序错误或传感器缺失不会自动重试。

自动评估使用 SC 同样的 13 个场景和 20 个新评估种子（20261301–20261320），每个模型 260 回合，共 1560 回合/936000 槽。保留实际提出/执行模式、尝试/成功/失败 DS、资源份额、交付质量、AoI 和共同奖励。评估逐槽独立核算资源、缓存、质量可行性和奖励；外生轨迹哈希按原 SC 的 CPU 计算顺序生成，并核对张量环境的信道和指令。不会将评估回合当作额外训练种子，不根据短测排名改参数或提前挑选最佳模型。

小预算 smoke 只检验程序、更新、配对、恢复和资源使用，不能证明 CC 或 SC 优势。正式输入冻结在每个 run/source 和 configs 中。所有历史表和运行中的 SC 源码保持不变。新环境复用 SC 的初始化与空间定义；旧 CC 类的 5 模式映射未被接入正式新流程。

科学范围是“固定 SC 平均模型与独立校准 CC 平均模型”的系统比较。SC 的真实编码器/输入/载荷历史绑定尚未完全恢复，不能据此声称真实编解码器层面已经严格公平。两者的传输成功抽象不同，应在论文中明确说明。对比结果必须在完整预算和同一外生轨迹评估后判断，不能由 profile 的质量和载荷范围推定。
