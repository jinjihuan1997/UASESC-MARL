2026-09-10：第二步，同一冻结模型的正确/隐藏显式指令闭环比较。事后运行时输入消融，不重训练。

冻结输入来自 three_seed_sc_20260909。使用 IC_HAPPO、IC_MAPPO 各 3 个最终 10M 步模型，训练种子 85、218、966；每个模型运行全部 13 个原评估场景、20 个评估种子（20261301–20261320）、600 槽。两种条件合计 3120 回合、1872000 槽。不按结果选择模型、场景或检查点。

correct：actor 正常接收真实指令编号及质量/AoI 门限。hidden_at_deployment：使用完全相同的已训练权重，在每次 actor 决策前把上述显式输入置零，观察长度不变。真实指令、质量门限、奖励权重、critic 配置、物理链路、缓存更新和筛选规则均不改变；待传载荷等其他观察仍由真实环境计算。代码核对每次干预只改指定坐标。没有错误指令分支，也不添加新训练。

每对条件具有相同初始状态、信道和任务随机轨迹。随后分别执行自己的动作，允许 AoI、缓存、分配和候选集中的缓存条件随轨迹自然分化；不能强行保持后续内生状态一样，否则不是闭环性能测试。实际信道和任务轨迹逐槽核对，并按原 CPU 参考的哈希规则与正式结果配对。所有条件均由真实指令评分，隐藏输入不会放宽要求或修改奖励。

使用冻结张量环境批量执行 20 个评估种子，每个分支采用相同的推理批次布局和确定性 actor。独立 NumPy 核算每槽缓存/AoI 转移、交付数、质量、资源、约束和共同奖励。正确输入分支与历史 CPU 单环境评估作逐槽校准：AoI、交付量保持一致，质量总和与资源的浮点容差为 1e-7/1e-6，奖励为 1e-9。若某个评估回合因批量浮点误差导致结果不匹配，对该回合的两个条件一起改用冻结 CPU 参考环境及原单环境 actor 推理重跑；必须恢复历史正确输入指标后才能提交。此规则只依赖正确分支的复现检查，不依赖干预的有利/不利结果。记录每次回退及原因。

主要比较：按训练种子配对的 correct−hidden_at_deployment 共同奖励差。同时报告 AoI、成功交付 PSNR、更新/槽、资源/槽及超限比例，按全部场景和真实指令分别展开。质量使用质量总和除以成功交付总数，不对每槽质量简单平均。每个训练种子先覆盖相同评估集合，再汇总三个种子的均值、标准差、范围和方向一致性。重复使用评估种子和场景不增加训练模型样本量；不宣称统计显著或最优性。

解释边界：这检验已经按完整输入训练的模型在运行时对显式输入的依赖。置零可能形成训练时没有出现的输入；即便 correct 优于 hidden_at_deployment，也不能替代“完整输入从头训练”与“隐藏输入从头训练”的正式消融，更不能直接证明新算法创新或必要性。既有独立训练的隐藏指令组作为历史背景另列，不能与本次临时遮挡组混称。

模型、平均表、冻结源码、原始评估及原稿全部只读；新输出仅写入本实验目录。两个 CPU 进程，各使用一个数学库线程，分别处理 HAPPO 与 MAPPO。
