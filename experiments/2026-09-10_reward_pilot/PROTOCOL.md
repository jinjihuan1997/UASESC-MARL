# 奖励接口修正与三种子短训对照

用户已授权按 2026-09-10_reward_audit 方案执行。本实验冻结两个任务：original_tail10 和 candidate_tail4；尾部阈值分别为 10、4，其余物理参数、平均质量表、指令权重、网络及 PPO 参数一致。4 是待验证的软服务目标，不是硬保证，也不根据最终测试排名再次调整。原模型、原输出和原稿保持不变。

生产评分统一供 CPU 环境、tensor 环境和一步规则预测使用；独立 numpy 物理/奖励核算保留。训练与评估始终使用完整 objective_reward，移除这个实验中未使用的辅助奖励开关。拆分奖励资源参考：reward_load_ref=60000、reward_load_scale=0.2，资源权重仍为 0.1，所以成本等价于旧 0.1×L/300000。观察 Lambda_ref=100000 不变。显示得分=100×原始效用，训练尺度=1，不启用潜势 shaping。

训练：每个任务下 IC_HAPPO、HAPPO_hidden_instruction，各种子 85、218、966，从头训练 1000000 环境步，共 12 个模型、12000000 步。每更新 10 环境×400槽，250次更新，按完整1M预算线性衰减。网络256×256、学习率1e-4/4e-4、5 PPO epochs、2 minibatches、clip0.1、gamma0.99、GAE0.95、entropy0.008，均不修改。仅末次模型作主评估。

资源：沿用已经实测更快的6个CPU作业、核心0—5、每进程1数学线程，共两批；同一训练种子/方法的两个奖励任务绑定同一核心。六作业基准和新代码训练冒烟计时仅用于运行时间，不看奖励选资源。每25更新保存恢复点。保留既有温控：CPU≥85°C持续5秒或≥95°C立即、GPU≥83°C暂停，冷却至少180秒且两者低于75°C持续60秒再恢复。只管理本实验进程，不新建应用监视自动任务。

校准：新种子20262001—20262010，固定9候选（模式0/5/10×均分/积压量/缓存AoI分配），在独立校准数据中为每个奖励任务冻结一个单一规则及按指令选档规则。校准只检查数值有效、存在可行交付与偏好取舍，并报告阈值激活和质量代价；不以RL排名或某个任务胜率改变阈值或筛掉不利任务。若规则家族中完全无法区分AoI/质量目标，则报告问题，不冒充已验证的服务目标。

最终评估：新种子20262201—20262220，继承13固定/切换指令场景、600槽。每任务评估6个模型与3个规则，共18对象×260回合=4680回合、2808000时隙。贪心规则使用对应完整目标的一步奖励。模型在其训练任务下执行，并对同一轨迹同时计算tail10、tail4效用；两任务仅改奖励，所以无需重复仿真即可按两个标准计分。无指令actor的配对物理轨迹必须跨指令相同；两任务/方法的外生轨迹必须配对。

统计：先在各训练种子内聚合，再报告3种子均值、标准差、显式减隐藏差值，以及同一模型方法tail4训练减tail10训练在两个效用口径下的配对差。报告AoI、预测交付PSNR、更新数、资源使用、A>4/A>6比例、各奖励分项。显示分数只是单位换算。规则无训练种子标准差。旧10M不是本轮1M对照。此为探索性候选验证，不以3/3正差代替统计显著性，不自动延长训练。

验证包含：原任务执行轨迹与冻结旧代码结果回放等价、两个新任务CPU/tensor一致、完整奖励的一步预测一致、非零罚系数和接收项路由、空缓存/不可行质量/预算边界、指令隐藏与切换、终止/rollout边界、训练恢复和小规模完整评估/汇总。所有正式输入在启动前冻结；意外失败保留记录，修正需明确记录，不能静默替换已开始的科学输入。
