# 质量收益权重减半：固定策略与匹配局部目标的诊断

用户明确选择将三种指令的质量收益权重降为原来的50%，AoI及资源成本保持原值。此协议在新结果产生前封存。只推理评估，无训练、拟合、种子或模型选择，无旧文件/论文/Git修改。

原[质量,AoI,资源]系数：均衡[0.5,0.4,0.1]；AoI[0.2,0.7,0.1]；质量[0.7,0.2,0.1]。
新系数：[0.25,0.4,0.1]、[0.1,0.7,0.1]、[0.35,0.2,0.1]。不重新归一化，否则AoI/资源项也被改变。原构造器检查行和为1，因此在本目录受控包装中先按原合法配置构造私人环境，再只替换该环境内存中的奖励系数。旧构造器、物理、阈值、观测、模式及执行器均不改；原奖励函数和每槽核验器直接使用新系数。

保留已完成轻量基线实验的全部13方法：四新基线、六旧基线、original_rl、C1、C3；学习控制器使用指定全部三个父模型与固定检查点。不重新训练，因此本轮衡量冻结策略对评价偏好的敏感性，不代表新奖励训练后的能力。

主要部分：19个固定控制器实例全部实际重跑13场景×20开发环境×600槽=4,940回合/2,964,000物理步。原方法名在本新目录中表示完全冻结其决策逻辑，只改变共同评分。每个冻结控制器必须与旧轨迹的请求动作和完整物理过程逐值相同，且新分数等于旧分数减去一半旧质量收益。

为避免只改变评分、让贪心继续按旧质量权重选模形成误导，额外封存四个明确后缀方法：G_equal_local16_Qhalf_local、G_urgency_local16_Qhalf_local、greedy_modes_16_Qhalf_local、greedy_modes_3_Qhalf_local。只将原UAVGreedy公开PublicSpec.weights同步为新系数，资源规则、候选、掩码、排序、调度和全部其余局部公式不变。各实际运行260回合，合计1,040回合/624,000步。两种旧强贪心的SUT预测器仍是旧奖励拟合的冻结预测器，绝不称为针对新目标重新拟合的完整强基线；这一局限与不拟合约束同时说明。新无拟合均分/紧急度贪心的局部评分已完整匹配新权重。

正式合计23个控制器实例，5,980完整回合，3,588,000物理步。不重新测延迟；本轮仅更换常数，无速度新结论。原延迟结果作为原实验数据保留。

预检：original_rl父模型104948945使用全部20种子在multi_0_1_2完整600槽；G_equal_local16、G_equal_local16_Qhalf_local、greedy_modes_16_Qhalf_local各用前2开发种子完整600槽；G_equal_local16_Qhalf_local前2种子重复一次。合计28完整回合/16,800物理步。正式前验证新权重准确且无归一化、只有质量项变化、观测不受系数直接影响、冻结/保存哈希、原策略物理重现、新贪心局部权重/公开接口一致、每槽与离线物理奖励重建、重复可复现。预检失败保留日志并修复，不能放宽容差。

全部环境只用原20个development validation种子与原13场景，600槽内保留完整切换闭环，不用reserved_final_test，不引入教师初始化gate_dev。相同种子共享外生序列和初态。原规则不按结果改映射，原强基线原文件原结果保持只读。

每槽保存指令、请求/执行模式、资源动作/份额/预算/用量、缓存/时间戳/AoI、交付、平均表预测质量、全部奖励分项及各局部观测/掩码。新目标和旧目标下的每条新轨迹得分均重算，以区分评价变化与行为变化。PSNR为固定平均表预测值，按交付加权。

统计：每槽共同奖励均值×100；13场景、20环境、三个父模型等权。固定任务、切换和逐父模型完整报告。bootstrap4000次，固定随机种子202609132105，环境为簇，保留其全部场景、方法、模型关联。区间只对当前模型与开发环境成立，不称训练总体结论，不把时隙/基线广播作为独立样本。

比较原RL/C1/C3与全部新旧目标下的基线；尤其同时呈现旧贪心仅换评分与局部贪心同步新权重的结果。变更共同目标后的排名改善不能证明RL学得更好，更不能证明原奖励必有bug。负结果保留；不根据结果继续降低权重。

CPU推理保持原dtype语义，Torch/BLAS线程1。从预检吞吐测量后采用至多3进程；不终止其他进程。所有原子写入限本目录，PYTHONDONTWRITEBYTECODE=1。协议/输入哈希匹配才能恢复未完成项。聚合完整重复两次，结束复查全部旧输入SHA和Git状态。完成本预算后停止，无追加训练、最终测试、论文修改或上传。
